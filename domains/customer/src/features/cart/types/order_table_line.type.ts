export type OrderTableLine = {
    id: number
    order_id: string
    table_id: number
    table_name: string
    reservation_date: string
    reservation_time: string
    guest_count: number
    created_at: string
}